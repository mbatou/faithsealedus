/** @type {import('next').NextConfig} */
const nextConfig = {
  // Gallery photos are served locally from /public, so no remote image hosts
  // need to be allowlisted.
};

export default nextConfig;
